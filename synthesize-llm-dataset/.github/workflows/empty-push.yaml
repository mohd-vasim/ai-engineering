name: Streamlit Keep Alive

permissions:
  contents: write

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"   # every hour

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout synthesize-finetuning-dataset branch
        uses: actions/checkout@v4
        with:
          ref: synthesize-finetuning-dataset
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create empty commit
        run: |
          git commit --allow-empty -m "chore: streamlit-keepalive [skip-ecr]"

      - name: Push commit
        run: |
          git push origin synthesize-finetuning-dataset